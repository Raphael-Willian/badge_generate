package com.example.badge.generate.repositorys;

import com.example.badge.generate.models.Crachas;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CrachasRepository extends JpaRepository<Crachas, Long> {
}